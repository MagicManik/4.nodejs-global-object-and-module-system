const os = require('os');

// to get os platform name
console.log(os.platform());
// to get os home directory name
console.log(os.homedir());
// to get free memory space of my os
console.log(os.freemem());
// to get cpus information'
console.log(os.cpus());
